import styled from "styled-components";
import { StyledContainer } from "../../ui-kit/Container/styles";

export const StyledAuthWrapper = styled(StyledContainer)`
  display: flex;
  flex-direction: column;
  align-items: center;

`;